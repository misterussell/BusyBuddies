import Activity from '../../../models/Activity';

describe('the Activity class', () => {
  it('should construct with an id property', () => {
    /*

    */
  });

  it('should set cognito user information for authorized db scans and puts', () => {
    /*
      Discovery is needed on this one. I know the reqs for DynamoDB
      but not amazon's SQL db requirements. Likely that they just need stamped
      with the existing user information.
    */
  })
});
